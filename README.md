The above files are related to deep learning models in python.The Convolutional neural network (CNN) model predicts the cat and dogs with an accuracy of 86%.The network uses Tensorflow and Keras.The model uses sequential framework and adam optimizer to adress the issue of overfitting.

The next model predicts the bank churn.Banks wants to know the retainability of the customers so that they can provide offers to them so that they as customers.The model with an accuracy of over 80% predicts whether certain customer with certain amount of bank balance would stay in the bank or leave the bank.
